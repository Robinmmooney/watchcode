var myName = 'Rob';

function sayName() {
    var secret = 'watchandcode';
    console.log(myName);
}
sayName(); // 'Rob'
console.log('secret');

//if you are inside of a function you can look out and see data, but the opposite isn't true. If you are outside you can't look in.

